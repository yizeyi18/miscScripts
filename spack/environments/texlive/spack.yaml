# This is a Spack Environment file.
#
# It describes a set of packages to be installed, along with
# configuration settings.
spack:
  # add package specs to the `specs` list
  specs:
  - texlive
    ^libtiff +ipo+lzma+pic+webp+zstd ^libwebp+gif+libwebpdecoder+libwebpdemux+libwebpextras+libwebpmux
    ^rust+dev
  view: true
  concretizer:
    unify: true
